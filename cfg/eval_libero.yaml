amplify_checkpoint: null
dataset:
  - libero_10

# Provide an absolute path to the LIBERO datasets
libero_path: null

max_steps: 500
temporal_agg: true

# Env
n_envs: 10

# Logging
use_wandb: false
# project_name: null
wandb_project: null
# entity_name: null
wandb_entity: null
# group_name: null
wandb_group: null
run_name: null

# Visualization
vis_traj: false # visualize predicted point tracks
img_size: 128

save_rollout_video: false
compile: false

# Optional: CTCLAI reranking
ctclai:
  enable: false
  checkpoint: null
  n_samples: 8
  lambda_tok: 1.0
  lambda_risk: 0.5
  lambda_prior: 0.1
  entropy_weighted_tok: true
  risk_discount: 0.99
